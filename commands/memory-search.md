---
description: Search the project memory knowledge base
argument-hint: <search query, e.g. "auth" or "database schema">
---

Search project memory for: "$ARGUMENTS"

Steps:
1. Use the memory skill to search for entries matching the query.
2. Return results grouped by type (decision, requirement, convention, etc.).
3. For each result show: the stored fact, when it was saved, and why it matters.

If no results are found, say so clearly and suggest a broader query.
If $ARGUMENTS is empty, list all memory categories and their entry counts.
