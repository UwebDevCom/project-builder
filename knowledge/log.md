# Knowledge Base Build Log

Append-only log of compiler-agent runs. Used to determine which daily logs are unprocessed.

---

_No compile runs yet. Run `/kb-compile` to process your first daily logs._
